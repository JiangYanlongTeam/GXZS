差旅费说明
1. 字段确认（差旅费）
	sfcjk 	是否冲销借款 					主表（0非是，1为否）
	cxje		冲销金额						明细表（只能有一条，js是否冲销借款为是时默认➕一条明细，不可编辑）
	jkdh		借款单号（浏览按钮）借款流程id	明细表（只能有一条，js是否冲销借款为是时默认➕一条明细，不可编辑）
	
2. 确认明细表几？
3. js联动
4. 字段确认（借款流程）
	hkztje	还款在途金额
	ljhkje	累计还款金额